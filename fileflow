#!/bin/bash

# Move to project root (ensures consistent paths)
cd "$(dirname "$0")"

# Activate virtual environment if it exists
if [ -d ".venv" ]; then
    source .venv/bin/activate
fi

# Run the app and pass through all CLI arguments
python3 -m app.main "$@"